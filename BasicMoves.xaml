<Page
    x:Class="ProjectApp.BasicMoves"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProjectApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <ImageBrush x:Key="BasicMovesBackground" 
                    ImageSource="Assets/Images/boxRing.jpg" Opacity="0.5"/>
    </Page.Resources>
    <Grid Background="{StaticResource BasicMovesBackground}">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top"
                    Margin="0,10,0,0" HorizontalAlignment="Center">
            <WebView x:Name="VideoLink" 
                     Width="375"
                     Height="330"/>
        </StackPanel>
        <StackPanel>
            <Button  x:Name="Back2"
                    HorizontalAlignment="Center"
                    Margin="0,400"
                    Content="Back" 
                    FontStyle="Italic"
                    Foreground="Chartreuse"
                    FontSize="25"
                    VerticalAlignment="Bottom" 
                    FontFamily="Arial Unicode MS" 
                    Click="Back2_Click">
            </Button>
        </StackPanel>
        <StackPanel>
            <Button  x:Name="Home"
                    HorizontalAlignment="Center"
                    Margin="0,500"
                    Content="Home" 
                    FontStyle="Italic"
                    Foreground="Chartreuse"
                    FontSize="25"
                    VerticalAlignment="Bottom" 
                    FontFamily="Arial Unicode MS" 
                    Click="Home_Click">
            </Button>
        </StackPanel>
    </Grid>
</Page>
